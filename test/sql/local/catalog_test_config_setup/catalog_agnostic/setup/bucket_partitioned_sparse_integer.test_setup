# Small fixture: reset on every include; independent of Spark-generated tables.

statement ok
DROP TABLE IF EXISTS my_datalake.default.slt_bucket_partitioned_sparse_integer;

statement ok
CREATE TABLE my_datalake.default.slt_bucket_partitioned_sparse_integer (
    id INTEGER,
    value INTEGER,
    name VARCHAR
)
PARTITIONED BY (bucket(16, value)) WITH ('format-version' = '2');

statement ok
INSERT INTO my_datalake.default.slt_bucket_partitioned_sparse_integer
VALUES
    (1,  1,  'alpha'),
    (2,  2,  'beta'),
    (3,  42, 'gamma'),
    (4,  1,  'alpha_dup'),
    (5,  42, 'gamma_dup');
