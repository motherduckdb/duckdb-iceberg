# Reset this small fixture for each test.

statement ok
DROP TABLE IF EXISTS my_datalake.default.table_properties;

statement ok
CREATE TABLE my_datalake.default.table_properties (a INTEGER) WITH ('format-version'='2', 'write.delete.mode'='merge-on-read');

statement ok
CALL set_iceberg_table_properties(my_datalake.default.table_properties, {'read.split.target-size': '268435456', 'foo': 'baz'});

statement ok
CALL remove_iceberg_table_properties(my_datalake.default.table_properties, ['foo']);

statement ok
CALL set_iceberg_table_properties(my_datalake.default.table_properties, {'duckdb_man': 'superman', 'another1': 'neato'});
