# name: test/sql/local/irc/insert/test_insert_incorrect_types.test
# group: [insert]

require-env ICEBERG_SERVER_AVAILABLE

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set enable_logging=true

statement ok
set logging_level='debug'

statement ok
CREATE SECRET (
    TYPE S3,
    KEY_ID 'admin',
    SECRET 'password',
    ENDPOINT '127.0.0.1:9000',
    URL_STYLE 'path',
    USE_SSL 0
);


statement ok
ATTACH '' AS my_datalake (
    TYPE ICEBERG,
    CLIENT_ID 'admin',
    CLIENT_SECRET 'password',
    ENDPOINT 'http://127.0.0.1:8181'
);


statement ok
create schema if not exists my_datalake.new_schema_1;

statement ok
drop table if exists my_datalake.new_schema_1.tinyint_table;

# statement ok
# create table my_datalake.new_schema_1.tinyint_table  as select range::TINYINT from range(10);

statement ok
create table my_datalake.new_schema_1.tinyint_table (a TINYINT);

statement ok
insert into my_datalake.new_schema_1.tinyint_table select range::TINYINT a from range(10);
