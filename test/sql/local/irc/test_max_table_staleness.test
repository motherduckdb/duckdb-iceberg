# name: test/sql/local/irc/test_max_table_staleness.test
# description: Test iceberg deletes
# group: [irc]

require-env ICEBERG_SERVER_AVAILABLE

require avro

require parquet

require iceberg

require httpfs

# Do not ignore 'HTTP' error messages!
set ignore_error_messages

statement ok
set logging_level='debug'

statement ok
CREATE SECRET (
    TYPE S3,
    KEY_ID 'admin',
    SECRET 'password',
    ENDPOINT '127.0.0.1:9000',
    URL_STYLE 'path',
    USE_SSL 0
);


statement ok
ATTACH '' AS my_datalake (
    TYPE ICEBERG,
    CLIENT_ID 'admin',
    CLIENT_SECRET 'password',
    ENDPOINT 'http://127.0.0.1:8181',
    MAX_TABLE_STALENESS 5
);

statement ok
CALL enable_logging('HTTP');

statement ok
FROM my_datalake.default.spark_written_upper_lower_bounds;

# request to table
# manifest file
# manifest list
# parquet file
query I
select count(*) from duckdb_logs_parsed('http');
----
4

statement ok
FROM my_datalake.default.spark_written_upper_lower_bounds;

# no new requests
query I
select count(*) from duckdb_logs_parsed('http');
----
4