name: Test Fixture Catalog Reusable
on:
  workflow_call:
    inputs:
      artifact-name:
        description: "Name of the DuckDB-Iceberg artifact to download"
        required: true
        type: string
      local-extension-repo:
        description: "Path to set as LOCAL_EXTENSION_REPO environment variable"
        required: false
        type: string
        default: ""
      equality-delete-writes-enabled:
        description: "Whether the downloaded artifact was built with ICEBERG_ENABLE_EQUALITY_DELETE_WRITES enabled (i.e. the Iceberg equality-delete write path is compiled in)."
        required: false
        type: number
        default: 0
      build-path:
        description: "The path to the build artifact"
        required: true
        type: string

jobs:
  test-spark:
    name: Test against fixture catalog
    runs-on: ubuntu-latest
    env:
      HTTP_PROXY_PUBLIC: http://localhost:8878
      ASAN_SYMBOLIZER_PATH: "/usr/bin/llvm-symbolizer"
      ASAN_OPTIONS: "detect_leaks=1:halt_on_error=1:abort_on_error=0:external_symbolizer_path=/usr/bin/llvm-symbolizer"
      LSAN_OPTIONS: "suppressions=${{ github.workspace }}/.combined-sanitizer-leak-suppressions.txt"
      UBSAN_OPTIONS: "halt_on_error=1:print_stacktrace=1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: 'true'

      - name: Download DuckDB-Iceberg artifact
        uses: actions/download-artifact@v8
        with:
          name: ${{ inputs.artifact-name }}
          path: build

      - name: Extract build artifact
        run: |
          rm -rf "${{ inputs.build-path }}"
          tar -xzf "${{ inputs.build-path }}-artifact.tar.gz" -C build
          ls -lh "${{ inputs.build-path }}"/test/unittest

      - name: Prepare leak suppressions
        run: |
          cp duckdb/.sanitizer-leak-suppressions.txt .combined-sanitizer-leak-suppressions.txt
          printf '\n' >> .combined-sanitizer-leak-suppressions.txt
          cat .sanitizer-leak-suppressions.txt >> .combined-sanitizer-leak-suppressions.txt

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Docker Compose
        uses: docker/setup-compose-action@v1

      - name: Install fixture catalog dependencies
        run: |
          sudo apt update -y -qq
          sudo apt install -y -qq python3-venv tar pkg-config llvm

      - name: Install CMake 3.x
        run: |
          sudo apt-get remove -y cmake cmake-data
          sudo apt-get install --allow-downgrades -y -qq 'cmake=3.*' 'cmake-data=3.*'

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Setup fixture
        run: |
          make fixture-data

      - name: Run mitmproxy
        shell: bash
        run: |
          source .venv-spark4/bin/activate
          mitmdump --mode regular@8878 --flow-detail 2 > mitmproxy.log 2>&1 &

      #- name: Install Iceberg & Run Generic Tests Set local extension repo, Install iceberg from build/release/extension and remove require iceberg from tests
      #  if: inputs.local-extension-repo != ''
      #  env:
      #    FIXTURE_SERVER_AVAILABLE: 1
      #  run: |
      #    # remove already installed extension if it is installed
      #    rm ~/.duckdb/extensions/**/**/iceberg.* || true
      #    echo "LOCAL_EXTENSION_REPO=${{ inputs.local-extension-repo }}" >> $GITHUB_ENV
      #    # check that we can load the extension
      #    ./build/release/duckdb -unsigned -c \
      #      "LOAD 'build/release/extension/iceberg/iceberg.duckdb_extension'; \
      #       INSTALL 'build/release/extension/iceberg/iceberg.duckdb_extension'; \
      #       LOAD 'build/release/extension/tpch/tpch.duckdb_extension'; \
      #       INSTALL 'build/release/extension/tpch/tpch.duckdb_extension'; \
      #       FROM duckdb_extensions() where extension_name in ('iceberg', 'tpch');"
      #    # remove require iceberg from tests
      #    grep -rl "require iceberg" test/sql/ | xargs sed -i '/^require iceberg$/d'
      #    # not sure why this is needed. Apparently tpch is also not linked
      #    grep -rl "require tpch" test/sql/ | xargs sed -i '/^require tpch$/d'
      #    ./build/release/test/unittest --order lex "$PWD/test/sql/local/catalog_test_config_setup/*" --list-test-names-only || true
      #    ./build/release/test/unittest --order lex "$PWD/test/sql/local/catalog_test_config_setup/*" --test-config test/configs/fixture.json --autoloading available

      - name: Run Generic IRC tests
        if: inputs.local-extension-repo == ''
        env:
          FIXTURE_SERVER_AVAILABLE: 1
          EQUALITY_DELETE_WRITES_ENABLED: ${{ inputs.equality-delete-writes-enabled }}
        run: |
          ${{ inputs.build-path }}/test/unittest --order lex "$PWD/test/sql/local/catalog_test_config_setup/*" --list-test-names-only || true
          ${{ inputs.build-path }}/test/unittest --order lex "$PWD/test/sql/local/catalog_test_config_setup/*" --test-config test/configs/fixture.json

      - name: Run other tests
        if: inputs.local-extension-repo == ''
        env:
          FIXTURE_SERVER_AVAILABLE: 1
        run: |
          ${{ inputs.build-path }}/test/unittest --order lex "$PWD/test/*" --list-test-names-only || true
          ${{ inputs.build-path }}/test/unittest --order lex "$PWD/test/*"

      - name: Test reads with PyIceberg and Spark
        if: inputs.local-extension-repo == ''
        env:
          FIXTURE_SERVER_AVAILABLE: 1
          EQUALITY_DELETE_WRITES_ENABLED: ${{ inputs.equality-delete-writes-enabled }}
        run: |
          # Only one SparkContext can live per JVM/process, so to genuinely test
          # multiple Iceberg runtimes we run the suite once per runtime, isolated
          # in its own process via the ICEBERG_RUNTIME selector.
          # test with spark v4 (Scala 2.13 runtimes)
          source .venv-spark4/bin/activate
          python3 -m pytest -vv test/python --unittest-binary ${{ inputs.build-path }}/test/unittest --test-python-verbosity verbose
          deactivate
          # test with spark v3 (Scala 2.12 runtimes)
          python3 -m venv .venv-spark3
          source .venv-spark3/bin/activate
          python3 -m pip install pyspark==3.5.2 pytest pyiceberg==0.9.1 pyarrow pydantic==2.9.0
          python3 -m pytest -vv test/python --unittest-binary ${{ inputs.build-path }}/test/unittest --test-python-verbosity verbose

      # The full duckdb/duckdb suite runs in test-fixture-catalog-duckdb-tests.yml
      # so that it can run in parallel with this workflow.
