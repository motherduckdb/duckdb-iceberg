diff --git a/examples/access-control-simple/docker-compose.yaml b/examples/access-control-simple/docker-compose.yaml
index 33ced8e8..08da0da8 100644
--- a/examples/access-control-simple/docker-compose.yaml
+++ b/examples/access-control-simple/docker-compose.yaml
@@ -4,10 +4,6 @@ services:
     depends_on:
       lakekeeper:
         condition: service_healthy
-      trino:
-        condition: service_healthy
-      starrocks:
-        condition: service_healthy
     command: start-notebook.sh --NotebookApp.token=''
     volumes:
       - ./notebooks:/home/jovyan/examples/
@@ -86,21 +82,6 @@ services:
     networks:
       iceberg_net:
 
-  trino:
-    image: trinodb/trino:476
-    environment:
-      - CATALOG_MANAGEMENT=dynamic
-    healthcheck:
-      test: ["CMD", "curl", "-I", "http://localhost:8080/v1/status"]
-      interval: 2s
-      timeout: 10s
-      retries: 2
-      start_period: 10s
-    ports:
-      - "9999:8080"
-    networks:
-      - iceberg_net
-
   migrate:
     image: ${LAKEKEEPER_TEST__SERVER_IMAGE:-quay.io/lakekeeper/catalog:latest-main}
     pull_policy: always
@@ -172,6 +153,8 @@ services:
       start_period: 15s
     volumes:
       - ./seaweedfs-iam.json:/etc/seaweedfs/iam.json:ro
+    ports:
+      - "8333:8333"
     networks:
       iceberg_net:
 
@@ -219,19 +202,5 @@ services:
       timeout: 30s
       retries: 3
 
-  starrocks:
-    image: starrocks/allin1-ubuntu:4.0.1
-    healthcheck:
-      test: ["CMD", "curl", "-I", "http://starrocks:8030/api/health"]
-      interval: 2s
-      timeout: 10s
-      retries: 2
-      start_period: 40s
-    ports:
-      - "9030:9030"
-      - "8030:8030"
-    networks:
-      - iceberg_net
-
 networks:
   iceberg_net:
