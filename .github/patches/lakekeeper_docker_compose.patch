diff --git a/examples/access-control-simple/docker-compose.yaml b/examples/access-control-simple/docker-compose.yaml
index 649a474a..4591d99f 100644
--- a/examples/access-control-simple/docker-compose.yaml
+++ b/examples/access-control-simple/docker-compose.yaml
@@ -4,10 +4,6 @@ services:
     depends_on:
       lakekeeper:
         condition: service_healthy
-      trino:
-        condition: service_healthy
-      starrocks:
-        condition: service_healthy
     command: start-notebook.sh --NotebookApp.token=''
     volumes:
       - ./notebooks:/home/jovyan/examples/
@@ -88,21 +84,6 @@ services:
     networks:
       iceberg_net:
 
-  trino:
-    image: trinodb/trino:476
-    environment:
-      - CATALOG_MANAGEMENT=dynamic
-    healthcheck:
-      test: ["CMD", "curl", "-I", "http://localhost:8080/v1/status"]
-      interval: 2s
-      timeout: 10s
-      retries: 2
-      start_period: 10s
-    ports:
-      - "9999:8080"
-    networks:
-      - iceberg_net
-
   migrate:
     image: ${LAKEKEEPER_TEST__SERVER_IMAGE:-quay.io/lakekeeper/catalog:latest-main}
     pull_policy: always
@@ -167,6 +148,9 @@ services:
       timeout: 10s
       retries: 2
       start_period: 15s
+    ports:
+      - "9000:9000"
+      - "9001:9001"
     networks:
       iceberg_net:
 
@@ -230,19 +214,5 @@ services:
       timeout: 30s
       retries: 3
 
-  starrocks:
-    image: starrocks/allin1-ubuntu:4.0.1
-    healthcheck:
-      test: ["CMD", "curl", "-I", "http://starrocks:8030/api/health"]
-      interval: 2s
-      timeout: 10s
-      retries: 2
-      start_period: 40s
-    ports:
-      - "9030:9030"
-      - "8030:8030"
-    networks:
-      - iceberg_net
-
 networks:
   iceberg_net:
